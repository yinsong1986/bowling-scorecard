(ns bowling-scorecard.core
  (:gen-class))

(defn new-scorecard
  "Create an empty score card"
  []
  (vector))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
